package org.openjfx.GameComponent;

public class PlayerEquipment extends GameComponent {
    /**
     * Constructor for the PlayerEquipment class
     * @param width double the width of the PlayerEquipment
     * @param height double the height of the PlayerEquipment
     * @param type String the type of PlayerEquipment
     */
    PlayerEquipment(double width, double height, String type) {
        super(width, height, type);
    }
}
